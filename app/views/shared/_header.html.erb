<header class="bg-star-dark w-full hーscreen">
  <div class="mx-auto max-w-full px-4 py-8 sm:px-6 sm:py-12 lg:px-8">
    <div class="flex items-center justify-between">
      <div class="flex flex-row">
        <h1 class="text-2xl font-russo text-white sm:text-3xl">
          <%=link_to "Link_Disco", root_path%>
        </h1>
        </div>
      <div class="flex items-center">
      <%= link_to "Go Home", root_path, data: {"turbolinks" => false}, class: "inline-flex items-center justify-center gap-1.5 rounded-lg bg-discord-dark px-5 py-3 text-white transition hover:bg-indigo-700 focus:outline-none" %>
      <%= link_to "Log Out", log_out_path,  data: { turbo_method: :delete }, class: "inline-flex items-center justify-center gap-1.5 rounded-lg bg-discord-dark px-5 py-3 text-white transition hover:bg-red-700 focus:outline-none" %>
      <%= link_to "Find Post", questions_path, class: "inline-flex items-center justify-center gap-1.5 rounded-lg bg-discord-dark px-5 py-3 text-white transition hover:bg-red-700 focus:outline-none mr-2 " %>
        <% if current_user.image.present? %>
          <%= image_tag current_user.image, alt: current_user.name, class: "font-dotgothic h-16 w-16 rounded-full" %>
        <%else%>
          <%= image_tag '1_test.png', alt: current_user.name, class: "font-dotgothic h-16 w-16 rounded-full" %>
        <% end %>
        <p class="ml-2 text-white font-dotgothic">
            <%= current_user.name %>
          </p>
      </div>
    </div>
  </div>
</header>